name: Release (generate changelog)

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write
  id-token: write

jobs:
  gen_changelog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'
          
      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate changelog with standard-version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          pnpm exec standard-version --infile CHANGELOG.md --no-verify # 💡 Switched to pnpm exec
          
      - name: Commit changelog
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # 💡 Switched to tracking pnpm-lock.yaml
          git add CHANGELOG.md package.json pnpm-lock.yaml || true 
          git commit -m "chore(release): update changelog [skip ci]" || echo "No changes to commit"
          git push origin HEAD:main
